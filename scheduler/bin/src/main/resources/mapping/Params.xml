<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huisheng.mapper.ParamsMapper">
  
  <!-- cache -->
  <cache type="com.huisheng.config.cache.MybatisRedisCache">
        <property name="eviction" value="LRU" />
        <property name="flushInterval" value="6000000" />
        <property name="size" value="1024" />
        <property name="readOnly" value="false" />
  </cache>

  <!--BaseResultMap-->
  <resultMap id="BaseResultMap" type="com.huisheng.model.Params">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="param_key" jdbcType="VARCHAR" property="paramKey" />
        <result column="param_value" jdbcType="VARCHAR" property="paramValue" />
  </resultMap>
  
  <!--columns-->
  <sql id="sql_columns">
      id, 
      name, 
      param_key, 
      param_value
  </sql>
  
  <!--where-->
  <sql id="sql_where">
    <where>
      <if test="null != id and '' != id">and id = #{id}</if>
      <if test="null != name and '' != name">and name = #{name}</if>
      <if test="null != paramKey and '' != paramKey">and param_key = #{paramKey}</if>
      <if test="null != paramValue and '' != paramValue">and param_value = #{paramValue}</if>
	</where>
  </sql>
  
  <select id="selectById" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from t_params where id = #{id}
  </select>
  
  <select id="selectOne" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from t_params <include refid="sql_where" />
  </select>
  
  <select id="selectByKey" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from t_params 
    where param_key=#{paramKey}
  </select>
  
  <select id="selectList" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from t_params <include refid="sql_where" />
  </select>
  
  <select id="selectPage" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from t_params <include refid="sql_where" /> limit #{page.startRow},#{page.pageSize}
  </select>
  
  <sql id="sql_save_columns">
    insert into t_params(
         <if test="null != name">name</if>,
         <if test="null != paramKey">param_key</if>,
         <if test="null != paramValue">param_value</if>
	) values
  </sql>
  
  <sql id="sql_save_values">
    (
     <if test="null != name"> #{name}</if> , 
     <if test="null != paramKey"> #{paramKey}</if> , 
     <if test="null != paramValue"> #{paramValue}</if>
	)
  </sql>
  
  <insert id="save" keyProperty="id" useGeneratedKeys="true">
    <include refid="sql_save_columns" /><include refid="sql_save_values" />
  </insert>
  
  <insert id="batchSave">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_save_columns" /><include refid="sql_save_values" />
	</foreach>
  </insert>
  
  <sql id="sql_update">
    update t_params set id = #{id}
     <if test="null != name">, name = #{name}</if>
     <if test="null != paramKey">, param_key = #{paramKey}</if>
     <if test="null != paramValue">, param_value = #{paramValue}</if>
	where id = #{id}
  </sql>
  
  <update id="update">
    <include refid="sql_update" />
  </update>
  
  <update id="batchUpdate">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_update" />
	</foreach>
  </update>
  
  <delete id="delArray">
    delete from t_params where id in
	<foreach collection="array" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  
  <delete id="delList">
    delete from t_params where id in
	<foreach collection="list" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  
</mapper>